{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "license": "${mod_license}",
  "icon": "multiblocked2additions.png",
  "environment": "*",
  "mixins": [
    "multiblocked2additions.fabric.mixins.json"
  ],
  "entrypoints": {
    "main": [
      "com.multiblocked2additions.fabric.FabricMod"
    ],
    "client": [
      "com.multiblocked2additions.fabric.FabricModClient"
    ]
  },
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": "${minecraft_version}",
    "ldlib": ">=${ldlib_version}"
  }
}
